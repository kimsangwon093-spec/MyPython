# 변수 >> 데이터를 담아 저장 하는 곳
# 변수 선언과 초기화 
# 변수 선언 >> 변수 이름을 정하는 것
# 변수 초기화 >> 변수 최초 값(초기값)을 정하는 것
num = 10 # >> num[변수선언] =["같다"가 아니라 대입 한다는 뜻이다] 10(변수 최초값)
# num은 10과 같다(X)
# num이 라는 이름의 변수(변수상자) 안에 10을 대입한다(V)
num_1 = 20
print(num_1)

string_1 = "최강 파이썬"
print(string_1)

bool_tf = True
print(bool_tf)
